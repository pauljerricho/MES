상속(Inheritance)
	-	부모 클래스가 자식 클래스에 자신이 가지고
	-	있는 필드와 메서드를 물려주는 행위
	
상속의 장점
	-	코드 재사용 가능
	-	부모와 자식의 관계를 만들어준다.
	
오버라이딩(Override)
	- 부모가 준 메서드를 재정의 --> 메서드 형태는 같으나 실행부분을 다시 구현함

super - 부모클래스를 지칭
	super() ---> 부모 생성자 호출
	super,메서드() ---> 부모가 가지고 있는 메서드를 실행
	
자식 클래스는 부모 클래스로 형변환이 가능하다.(업 캐스팅)
부모 클래스가 자식 클래스로 형변환이 원본이 자식이였을 때 가능하다.